apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cadence

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - ingress.yaml

images:
  - name: ghcr.io/sentient-soup/cadence
    newTag: latest   # CI rewrites this line on every deploy
